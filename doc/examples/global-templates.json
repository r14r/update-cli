{
  "schemaVersion": 1,
  "templates": [
    {
      "name": "Docker-Production",
      "description": "Aktualisiert Images und startet den Compose-Stack im Hintergrund.",
      "setup": {
        "commands": [
          "docker compose pull",
          "docker compose up -d --remove-orphans"
        ]
      }
    }
  ]
}
